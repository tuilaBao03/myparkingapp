import 'package:flutter/material.dart';

class AppColors {
  // Màu chính
  static const Color primary = Color(0xFF6200EE);
  static const Color primaryDark = Color(0xFF3700B3);

  // Màu nền
  static const Color background = Color.fromARGB(150, 245, 245, 245);
  static const Color backgroundDark = Color(0xFF121212);

  // Màu phụ
  static const Color secondary = Color(0xFF03DAC6);
  static const Color error = Color(0xFFB00020);

  static const secondaryColor = Color(0xFF2A2D3E);
  static const bgColor = Color(0xFF212332);

  // Các màu khác phổ biến
  static const Color success = Color(0xFF4CAF50); // Màu thành công (thường dùng cho các thông báo thành công)
  static const Color warning = Color(0xFFFFC107); // Màu cảnh báo (thường dùng cho các thông báo cảnh báo)
  static const Color info = Color(0xFF2196F3); // Màu thông tin (thường dùng cho các thông báo thông tin)
  static const Color darkGray = Color(0xFF616161); // Màu xám đậm
  static const Color lightGray = Color(0xFFBDBDBD); // Màu xám sáng
  static const Color white = Color(0xFFFFFFFF); // Màu trắng
  static const Color black = Color(0xFF000000); // Màu đen

  // Màu cho text
  static const Color lightText = Color(0xFFF5F5F5); // Màu chữ sáng
  static const Color darkText = Color(0xFF121212);  // Màu chữ tối

  // Màu cho các hiệu ứng (hover, pressed, focus)
  static const Color hoverColor = Color(0xFFBB86FC); // Màu hiệu ứng hover
  static const Color focusColor = Color(0xFF03DAC6); // Màu hiệu ứng focus



}